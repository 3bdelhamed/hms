{
  "name": "hms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:regression": "playwright test --grep @regression",
    "test:guests": "playwright test --grep @guests",
    "test:housekeeping": "playwright test --grep @housekeeping",
    "test:pricing": "playwright test --grep @pricing",
    "test:master-data": "playwright test --grep @master-data",
    "report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.6.2",
    "dotenv": "^18.0.1",
    "typescript": "^7.0.2"
  }
}
